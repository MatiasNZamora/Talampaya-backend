version: '3.3'

services:
  postgres:
    image: postgres:13
    environment:
      - PORTGRES_DB=my_db
      - PORTGRES_USER=root
      - PORTGRES_PASSWORD=123456
    ports:
      - '5432:5432'
    volumes:
      - ./postgres_data:/var/lib/postgresql/data